# Lock runtime — généré depuis pyproject.toml (python 3.12).
# Régénérer : pip install <deps de pyproject> puis `pip freeze`.
# Attention, pip freeze aplatit les marqueurs de plateforme. Après une
# régénération, remettre le marqueur de uvloop, sans quoi l'installation
# échoue sous Windows faute de roue.
#
# NE PAS DÉ-ÉPINGLER fastapi / pydantic / uvicorn / starlette SANS PR DE CONTRAT.
#
# fastapi et pydantic sont épinglés à la version exacte parce que ce sont eux
# qui génèrent la spécification OpenAPI. Une montée de version, même en patch,
# peut produire un JSON différent (ordre, titres générés, forme des anyOf,
# version d'OpenAPI émise) alors qu'aucun DTO n'a bougé. Le job CI
# contract-drift comparerait alors le code au contrat gelé et échouerait sur
# une dérive qui n'existe pas, et une régénération du contrat masquerait la
# vraie cause en la faisant passer pour une évolution métier.
#
# Pour monter fastapi ou pydantic : ouvrir une PR sur
# enervision/docs/contracts qui contient à la fois le bump de version ici et
# le contrat régénéré, et la faire relire par les trois consommateurs comme
# n'importe quelle évolution de contrat.
alembic==1.19.1
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
argon2-cffi==25.1.0
argon2-cffi-bindings==26.1.0
asyncpg==0.30.0
certifi==2026.7.22
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
ecdsa==0.19.2
fastapi==0.121.2
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
Mako==1.4.1
MarkupSafe==3.0.3
psycopg==3.3.5
psycopg-binary==3.3.5
pyasn1==0.6.4
pycparser==3.0
pydantic==2.12.4
pydantic-settings==2.15.0
pydantic_core==2.41.5
python-dotenv==1.2.3
python-jose==3.5.0
python-multipart==0.0.20
PyYAML==6.0.3
rsa==4.9.1
six==1.17.0
SQLAlchemy==2.0.52
starlette==0.49.3
typing-inspection==0.4.4
typing_extensions==4.16.0
uvicorn==0.38.0
uvloop==0.22.1 ; sys_platform != "win32" and sys_platform != "cygwin" and platform_python_implementation != "PyPy"
watchfiles==1.2.0
websockets==17.1
